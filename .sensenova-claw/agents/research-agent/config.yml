name: Research Agent
description: 按指定维度搜集证据，输出带引用的子报告

temperature: 0.2

tools: ["serper_search", "brave_search", "tavily_search", "fetch_url", "image_search", "read_file"]
skills: ["union-search-skill"]

can_send_message_to: null
enabled: true
