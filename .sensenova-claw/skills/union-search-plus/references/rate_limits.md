# 速率与成本建议

## 原则

- 优先主链，补充链只在不足时触发
- 每次补充优先 `preferred`
- 避免在同一轮重复触发 `all`

## 建议参数

- `preferred`：`--limit 5 --timeout 60`
- `all`：`--limit 5 --timeout 90`

## 预算控制

- 当主链已满足阈值时，不触发补充
- 对同一查询结果做本地去重，避免重复抓取
