# 创建telegram bot

### 1. 找到 @BotFather（[直达链接](https://t.me/BotFather)）
确认用户名确实是 @BotFather

### 2. 在聊天框发送`/newbot`

### 3. 给bot取名
不能重复

### 4. 获取token
取完名自动生成带token的文本（复制`Use this token to access the HTTP API:`）

或输入`/token`,然后选取bot获取token